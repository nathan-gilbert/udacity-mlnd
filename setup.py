from setuptools import setup

setup(name="distributions",
      version="0.1",
      description="A simple package for probability distributions.",
      package=['distributions'],
      zip_safe=False)
